#include <stdio.h>


int main(int argc, char const *argv[]) {
   /* code */
   int tests;
   scanf("%d", &tests);

   while (tests) {
      int numCards;
      scanf("%d", numCards);
      while (numCards) {
         
      }

      tests--;
   }

   return 0;
}
